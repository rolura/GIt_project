#September 14th class

print("hello")

git_sitrep()

#tells you what you never want to commit and their location
git_vaccinate()
#Do not share your token
create_github_token()
gitcreds::gitcreds_set() #add the token you just created

#Run git_sitrep again to see what have changes
git_sitrep()

#Project does not uses Github so run this command
use_github()

#if you change something you need to commit the changes and click the push buttom on the ennvironment-git



